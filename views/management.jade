extends layout
block content
  script(src='/modules/controllers.management.js')
  .app-wrapper(ng-app='management')
    #wrapper
      #header-wrapper
        #header
          #logo
            h1
              a(href='#') Duo Tao's Blog 
            p
              |  Administrator Page
      // end #header
      #menu(ng-controller='menuController')
        include partials/managementMenu
      // end #menu
      #page
        #page-bgtop
          #page-bgbtm
            #overview(ng-if="content === 'overview'")
              include partials/overview
            #post(ng-if="content === 'post'")
              include partials/post
            #locations(ng-if="content === 'location'")
              include partials/location
            div(style='clear: both;')
      // end #page
    #footer
      include partials/footer
